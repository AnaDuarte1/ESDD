#include "media.h"

float media (int x, int y, float p1, float p2){
    float media;
    return media = (((x * p1) + (y * p2)) / (p1+p2));
}

int somaVetor (int vetor[]){
    int i;
    int soma;

    for(i = 0; i < 5; i++){
        return soma = soma  + vetor[i];
    }
}